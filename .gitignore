# Recordings and logs (never commit captured video)
*.mkv
*.avi
*.raw
*.yuv
*.tegrastats.log
thermal_*.log

# Python
__pycache__/
*.pyc
.venv/
venv/

# Calibration: keep the scripts + *_intrinsics.json + stereo_extrinsics.json;
# ignore the snapshot images and the generated board image
calibration/images/
charuco_board.png
*_undistort_sample.jpg
*_rectified_sample.jpg

# Stitching: keep source (stitch_pipeline.cpp/CMakeLists/include);
# ignore build output and generated renders
stitching/build/
stitching/pipeline_out*/
*.bin

# 3D model: commit only the zip; ignore the loose CAD files (incl. the 53 MB STEP)
3d-housing-model/*
!3d-housing-model/Housing.zip

# Editor / OS
.vscode/
.idea/
.DS_Store
